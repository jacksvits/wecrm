export interface Role { id: string; name: string; label: string; color: string; textColor: string; sortOrder: number; allowedPages: string[]; isAssignable: boolean; notifyOnNewTask?: boolean; notifyOnNewDeal?: boolean; notifyOnNewComment?: boolean; notifyOnNewChat?: boolean; notifyOnNewCall?: boolean; notifyOnNewNews?: boolean; } export interface Status { id: string; entityType: 'task' | 'project' | 'deal'; name: string; label: string; color: string; textColor: string; sortOrder: number; isDefault: boolean; isActive: boolean; } export interface User { id: string; email: string; username?: string | null; avatar?: string | null; name: string; emails?: string[]; roleId?: string; role?: Role | string; hideCompletedTasks?: boolean; darkTheme?: boolean; pushEnabled?: boolean; notifyTask?: boolean; notifyComment?: boolean; notifyChat?: boolean; notifyCall?: boolean; notifyDeal?: boolean; notifyNews?: boolean; soundEnabled?: boolean; allowedPages?: string[]; lastActiveAt?: string; canBeCurator?: boolean; novofonExtension?: string | null; } export interface FileAttachment { id: string; filename: string; originalName: string; mimeType: string; size: number; path: string; entityType: string; entityId: string; authorId: string; author?: { id: string; name: string; avatar?: string }; createdAt: string; } export interface Task { id: string; title: string; description?: string; status: string; priority: string; dueDate?: string; createdAt: string; assigneeId?: string | null; assignee?: { id: string; name: string; avatar?: string } | null; assignees?: { user: { id: string; name: string; avatar?: string } }[]; assigneeIds?: string[]; curatorIds?: string[]; curators?: { id: string; name: string; avatar?: string }[]; creatorId?: string; creator?: { id: string; name: string }; projectId?: string; project?: { id: string; name: string }; contactId?: string; contact?: { id: string; name: string; company?: string }; dealId?: string; deal?: { id: string; title: string; value: number }; parentId?: string | null; parent?: { id: string; title: string; status: string } | null; children?: Task[]; comments?: Comment[]; attachments?: FileAttachment[]; sourceEmail?: string; emailMessageId?: string; _count?: { comments: number; children: number }; history?: TaskHistory[]; ticketNumber?: number; } export interface TaskHistory { id: string; taskId: string; field: string; oldValue?: string | null; newValue?: string | null; userId?: string | null; user?: { id: string; name: string; avatar?: string } | null; createdAt: string; } export interface Contact { id: string; name: string; email?: string; phone?: string; emails: string[]; phones: string[]; company?: string; type: string; kind: 'contact' | 'organization'; tags: string[]; notes?: string; avatarUrl?: string; maxUserId?: string; description?: string; lastActivityTime?: string; inn?: string; ogrn?: string; legalAddress?: string; position?: string; organizationId?: string | null; organization?: { id: string; name: string; inn?: string; phone?: string } | null; employees?: { id: string; name: string; position?: string; phone?: string; email?: string }[]; createdAt: string; _count?: { deals: number; tasks: number; employees?: number }; } export interface Project { id: string; name: string; description?: string; status: string; startDate?: string; endDate?: string; progress: number; createdAt: string; parentId?: string | null; parent?: { id: string; name: string } | null; children?: Project[]; _count?: { tasks: number; deals: number }; isLocked?: boolean; } export interface Deal { id: string; title: string; value: number; currency: string; stage: string; probability: number; expectedClose?: string; createdAt: string; updatedAt: string; contactId?: string; contact?: { id: string; name: string; company?: string }; projectId?: string; project?: { id: string; name: string }; } export interface Comment { id: string; content: string; createdAt: string; authorId: string; author?: { id: string; name: string; avatar?: string }; taskId: string; attachments?: FileAttachment[]; isInternal?: boolean; } export interface Activity { id: string; action: string; entity: string; entityId: string; entityName?: string | null; details?: string; createdAt: string; userId?: string; user?: { id: string; name: string }; taskId?: string; task?: { id: string; title: string }; } export interface DashboardStats { totalTasks: number; totalContacts: number; totalDeals: number; totalProjects: number; tasksByStatus: { open: number; in_progress: number; done: number; cancelled: number }; dealsByStage: { lead: number; negotiation: number; proposal: number; closed_won: number; closed_lost: number }; recentActivities: Activity[]; metrics: { activeTasks: number; totalDealValue: number; overdueTasks: number; users: number; onlineUsers: number; }; onlineUsersList: Array<{ id: string; name: string; avatar?: string | null }>; pipeline: Array<{ stage: string; _count: { id: number } }>; } export interface EmailSettings { id: string; imapHost: string; imapPort: number; imapUser: string; imapPass: string; checkIntervalMs: number; processedFolder?: string; defaultCreatorId?: string; isActive: boolean; secure: boolean; rejectUnauthorized: boolean; requireTLS: boolean; createdAt: string; updatedAt: string; hasPassword?: boolean; } export interface ChatMessage { id: string; content: string; createdAt: string; authorId: string; author?: { id: string; name: string; avatar?: string }; readBy?: Array<{ userId: string; user?: { name: string } }>; attachments?: FileAttachment[]; deletedAt?: string; reactions?: Array<{ emoji: string; users: string[] }>; recipients?: Array<{ userId: string; user?: { name: string } }>; replyTo?: ChatMessage | null; replyToId?: string; } export interface PushSubscription { endpoint: string; p256dh: string; auth: string; } export interface Notification { id: string; title: string; body: string; url?: string; readAt?: Date; createdAt: Date; } export interface TelephonySettings { id: string; isActive: boolean; apiKey: string; apiSecret?: string; virtualNumber?: string; webhookSecret?: string; autoCreateContact: boolean; autoCreateTask: boolean; autoCreateTaskOnIncoming: boolean; autoCreateTaskOnOutgoing: boolean; autoAttachRecord: boolean; notifyAdminsOnSms: boolean; defaultUserId?: string | null; defaultIovEmployeeId?: number | null; defaultIovEmployeeName?: string | null; hasSecret?: boolean; createdAt: string; updatedAt: string; } export interface Call { id: string; pbxCallId: string; callIdWithRec?: string; direction: 'incoming' | 'outgoing'; callerId: string; calledDid: string; internal?: string; employeeName?: string; duration: number; disposition: 'answered' | 'missed' | 'busy' | 'failed' | 'unknown' | 'in_progress'; statusCode?: string; isRecorded: boolean; recordUrl?: string; callStart: string; callEnd?: string; contactId?: string; contact?: { id: string; name: string; company?: string }; taskId?: string; task?: { id: string; title: string }; notes?: string; tags: string[]; createdAt: string; } export interface NovofonEmployee { id: number; name: string; email: string; phone_numbers: { number: string; status: string; sip_login?: string }[]; virtual_phone_number?: string; }
// News System Types
export interface NewsCategory { id: string; name: string; slug: string; color: string; sortOrder: number; subcategories?: NewsSubcategory[]; }
export interface NewsSubcategory { id: string; name: string; slug: string; categoryId: string; }
export interface NewsTag { id: string; name: string; slug: string; color: string; }
export interface NewsHistory { id: string; newsId: string; title: string; summary?: string; content: string; labels: string[]; categoryId?: string; subcategoryId?: string; tagIds: string[]; coverImage?: string; isPublished: boolean; editorName: string; createdAt: string; }
export interface News { id: string; title: string; slug: string; summary?: string; content: string; coverImage?: string; isPublished: boolean; publishedAt?: string; views: number; labels: string[]; authorId: string; author?: { id: string; name: string; avatar?: string }; categoryId?: string; category?: NewsCategory; subcategoryId?: string; subcategory?: NewsSubcategory; tags: NewsTag[]; history: NewsHistory[]; createdAt: string; updatedAt: string; }

export interface TaskTransaction {
  id: string;
  taskId: string;
  type: 'income' | 'expense';
  amount: number;
  description?: string;
  date: string;
  createdAt: string;
  updatedAt: string;
}

export interface TaskFinances {
  budget: number;
  price: number;
  totalIncome: number;
  totalExpense: number;
  profit: number;
  transactions: TaskTransaction[];
}
export interface VkGroupSettings { id: string; groupId: number; accessToken: string; defaultCreatorId?: string | null; assigneeIds: string[]; autoCreateContact: boolean; isActive: boolean; lastTs?: number | null; callbackSecret?: string | null; confirmationString?: string | null; createdAt: string; updatedAt: string; }

export interface SmsMessage {
  id: string;
  from: string;
  to: string;
  text: string;
  contactId?: string | null;
  contact?: { id: string; name: string } | null;
  receivedAt: string;
  createdAt: string;
}