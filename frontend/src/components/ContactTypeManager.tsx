import { useEffect, useState } from 'react' ; import { api } from '../api/client' ; import { useAuth } from '../context/AuthContext' ; interface ContactType { id: string; name: string; label: string; color: string; textColor: string; sortOrder: number; isActive: boolean; } export function ContactTypeManager() { const { user } = useAuth() ; const isAdmin = user?.role === 'admin' ; const [types, setTypes] = useState<ContactType[]>([]) ; const [loading, setLoading] = useState(true) ; const [showForm, setShowForm] = useState(false) ; const [editingId, setEditingId] = useState<string | null>(null) ; const [form, setForm] = useState({ name: '', label: '', color: '#e8f5e9', textColor: '#2e7d32', sortOrder: 0, isActive: true }) ; useEffect(() => { loadTypes() ; }, []) ; const loadTypes = async () => { setLoading(true) ; try { const data = await api.contactTypes.list() ; setTypes(data) ; } catch (err) { console.error('Failed to load contact types:', err) ; } finally { setLoading(false) ; } } ; const openCreate = () => { setEditingId(null) ; setForm({ name: '', label: '', color: '#e8f5e9', textColor: '#2e7d32', sortOrder: types.length, isActive: true }) ; setShowForm(true) ; } ; const openEdit = (t: ContactType) => { setEditingId(t.id) ; setForm({ name: t.name, label: t.label, color: t.color, textColor: t.textColor, sortOrder: t.sortOrder, isActive: t.isActive }) ; setShowForm(true) ; } ; const handleSubmit = async (e: React.FormEvent) => { e.preventDefault() ; try { if (editingId) { await api.contactTypes.update(editingId, form) ; } else { await api.contactTypes.create(form) ; } setShowForm(false) ; loadTypes() ; } catch (err: any) { alert(err.message || 'Ошибка сохранения') ; } } ; const handleDelete = async (id: string) => { if (!confirm('Удалить тип контакта?')) return ; try { await api.contactTypes.delete(id) ; loadTypes() ; } catch (err: any) { alert(err.message || 'Ошибка удаления') ; } } ; if (loading) return <div style={{ padding: 40, textAlign: 'center' }}>Загрузка...</div> ; return ( <div> <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 16 }}> <h3 style={{ margin: 0, fontSize: 16 }}>Типы контактов</h3> {isAdmin && ( <button onClick={openCreate} style={{ padding: '8px 16px', borderRadius: 12, border: 'none', background: 'var(--text-primary)', color: 'var(--bg-card)', cursor: 'pointer', fontSize: 14 }}>+ Добавить</button> )} </div> {showForm && ( <form onSubmit={handleSubmit} style={{ marginBottom: 20, padding: 20, borderRadius: 16, border: '1px solid var(--border-color)', background: 'var(--bg-card)' }}> <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: 12, marginBottom: 12 }}> <div> <label style={{ fontSize: 12, color: 'var(--text-muted)', display: 'block', marginBottom: 4 }}>Системное имя (en)</label> <input value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} required style={{ width: '100%', padding: 10, borderRadius: 12, border: '1px solid var(--border-color)', background: 'var(--bg-input)', color: 'var(--text-primary)' }} /> </div> <div> <label style={{ fontSize: 12, color: 'var(--text-muted)', display: 'block', marginBottom: 4 }}>Название</label> <input value={form.label} onChange={e => setForm({ ...form, label: e.target.value })} required style={{ width: '100%', padding: 10, borderRadius: 12, border: '1px solid var(--border-color)', background: 'var(--bg-input)', color: 'var(--text-primary)' }} /> </div> <div> <label style={{ fontSize: 12, color: 'var(--text-muted)', display: 'block', marginBottom: 4 }}>Цвет фона</label> <input type='color' value={form.color} onChange={e => setForm({ ...form, color: e.target.value })} style={{ width: '100%', height: 40, borderRadius: 12, border: '1px solid var(--border-color)', cursor: 'pointer' }} /> </div> <div> <label style={{ fontSize: 12, color: 'var(--text-muted)', display: 'block', marginBottom: 4 }}>Цвет текста</label> <input type='color' value={form.textColor} onChange={e => setForm({ ...form, textColor: e.target.value })} style={{ width: '100%', height: 40, borderRadius: 12, border: '1px solid var(--border-color)', cursor: 'pointer' }} /> </div> <div> <label style={{ fontSize: 12, color: 'var(--text-muted)', display: 'block', marginBottom: 4 }}>Порядок</label> <input type='number' value={form.sortOrder} onChange={e => setForm({ ...form, sortOrder: parseInt(e.target.value) || 0 })} style={{ width: '100%', padding: 10, borderRadius: 12, border: '1px solid var(--border-color)', background: 'var(--bg-input)', color: 'var(--text-primary)' }} /> </div> </div> <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 12 }}> <input type='checkbox' id='ct-active' checked={form.isActive} onChange={e => setForm({ ...form, isActive: e.target.checked })} /> <label htmlFor='ct-active' style={{ fontSize: 14, cursor: 'pointer' }}>Активен</label> </div> <div style={{ display: 'flex', gap: 8, justifyContent: 'flex-end' }}> <button type='button' onClick={() => setShowForm(false)} style={{ padding: '8px 16px', borderRadius: 12, border: '1px solid var(--border-color)', background: 'var(--bg-card)', cursor: 'pointer', color: 'var(--text-primary)' }}>Отмена</button> <button type='submit' style={{ padding: '8px 16px', borderRadius: 12, border: 'none', background: 'var(--text-primary)', color: 'var(--bg-card)', cursor: 'pointer' }}>{editingId ? 'Сохранить' : 'Добавить'}</button> </div> </form> )} <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}> {types.length === 0 && <div style={{ color: 'var(--text-muted)', fontSize: 13 }}>Нет типов контактов</div>} {types.map(t => ( <div key={t.id} style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '12px 16px', borderRadius: 12, border: '1px solid var(--border-color)', background: 'var(--bg-card)' }}> <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}> <span style={{ padding: '4px 12px', borderRadius: 8, fontSize: 12, fontWeight: 600, background: t.color, color: t.textColor }}>{t.label}</span> <span style={{ fontSize: 12, color: 'var(--text-muted)' }}>{t.name}</span> {!t.isActive && <span style={{ padding: '2px 8px', borderRadius: 6, fontSize: 11, background: '#fee2e2', color: '#991b1b' }}>Неактивен</span>} </div> {isAdmin && ( <div style={{ display: 'flex', gap: 6 }}> <button onClick={() => openEdit(t)} style={{ padding: '4px 10px', borderRadius: 8, border: '1px solid var(--border-color)', background: 'var(--bg-card)', cursor: 'pointer', fontSize: 12 }}>✏️</button> <button onClick={() => handleDelete(t.id)} style={{ padding: '4px 10px', borderRadius: 8, border: '1px solid var(--border-color)', background: 'var(--bg-card)', cursor: 'pointer', fontSize: 12, color: '#dc2626' }}>🗑</button> </div> )} </div> ))} </div> </div> ) ; }