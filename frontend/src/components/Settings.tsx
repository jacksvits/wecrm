import { useState } from 'react' ; import { EmailSettings } from './EmailSettings' ; import { RoleManager } from './RoleManager' ; import { TelephonySettings } from './TelephonySettings' ; import { StatusManager } from './StatusManager' ; import { MaxSettings } from './MaxSettings' ; import { UserList } from './UserList' ; import { VkGroupSettings } from './VkGroupSettings' ; import { ContactTypeManager } from './ContactTypeManager' ; import { SmsJournal } from './SmsJournal' ; type MainTab = 'roles' | 'statuses' | 'users' | 'contactTypes' | 'integrations' ; type IntegrationSubTab = 'email' | 'telephony' | 'max' | 'vk' | 'sms' ; export function Settings() { const [activeTab, setActiveTab] = useState<MainTab>('roles') ; const [integrationSubTab, setIntegrationSubTab] = useState<IntegrationSubTab>('email') ; const tabStyle = (isActive: boolean): React.CSSProperties => ({ padding: '10px 20px', borderRadius: 12, border: 'none', background: isActive ? 'var(--text-primary)' : 'transparent', color: isActive ? 'var(--bg-card)' : 'var(--text-secondary)', fontSize: 14, fontWeight: 500, cursor: 'pointer', transition: 'all 0.2s', }) ; const subTabStyle = (isActive: boolean): React.CSSProperties => ({ padding: '8px 16px', borderRadius: 10, border: '1px solid var(--border-color)', background: isActive ? 'var(--bg-input)' : 'transparent', color: isActive ? 'var(--text-primary)' : 'var(--text-secondary)', fontSize: 13, fontWeight: 500, cursor: 'pointer', transition: 'all 0.2s', }) ; const renderContent = () => { switch (activeTab) { case 'roles': return <RoleManager /> ; case 'statuses': return <StatusManager /> ; case 'users': return <UserList /> ; case 'contactTypes': return <ContactTypeManager /> ; case 'integrations': return renderIntegrations() ; default: return null ; } } ; const renderIntegrations = () => { return ( <div> <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap', padding: '12px 16px', borderRadius: 12, background: 'var(--bg-input)', border: '1px solid var(--border-color)' }}> <button style={subTabStyle(integrationSubTab === 'email')} onClick={() => setIntegrationSubTab('email')}>Почта</button> <button style={subTabStyle(integrationSubTab === 'telephony')} onClick={() => setIntegrationSubTab('telephony')}>Телефония</button> <button style={subTabStyle(integrationSubTab === 'max')} onClick={() => setIntegrationSubTab('max')}>MAX</button> <button style={subTabStyle(integrationSubTab === 'vk')} onClick={() => setIntegrationSubTab('vk')}>ВК Группа</button> <button style={subTabStyle(integrationSubTab === 'sms')} onClick={() => setIntegrationSubTab('sms')}>SMS журнал</button> </div> {integrationSubTab === 'email' && <EmailSettings />} {integrationSubTab === 'telephony' && <TelephonySettings />} {integrationSubTab === 'max' && <MaxSettings />} {integrationSubTab === 'vk' && <VkGroupSettings />} {integrationSubTab === 'sms' && <SmsJournal />} </div> ) ; } ; return ( <div> <h2 style={{ fontSize: 20, fontWeight: 600, marginBottom: 16 }}>Настройки</h2> <div style={{ display: 'flex', gap: 8, marginBottom: 24, flexWrap: 'wrap' }}> <button style={tabStyle(activeTab === 'roles')} onClick={() => setActiveTab('roles')}>Роли</button> <button style={tabStyle(activeTab === 'statuses')} onClick={() => setActiveTab('statuses')}>Статусы</button> <button style={tabStyle(activeTab === 'users')} onClick={() => setActiveTab('users')}>Пользователи</button> <button style={tabStyle(activeTab === 'contactTypes')} onClick={() => setActiveTab('contactTypes')}>Типы контактов</button> <button style={tabStyle(activeTab === 'integrations')} onClick={() => setActiveTab('integrations')}>Интеграции</button> </div> {renderContent()} </div> ) ; }