import { useEffect, useState } from 'react';;import { api } from '../api/client';;import { SmsMessage } from '../types';;export function SmsJournal() { const [items, setItems] = useState<SmsMessage[]>([]); const [total, setTotal] = useState(0); const [search, setSearch] = useState(''); const [loading, setLoading] = useState(false); const load = async () => { setLoading(true); try { const res = await api.telephony.getSms(search ? `search=${encodeURIComponent(search)}` : undefined); setItems(res.items); setTotal(res.total); } catch (e: any) { console.error(e); } finally { setLoading(false); } }; useEffect(() => { load(); }, []); const handleSearch = (e: React.FormEvent) => { e.preventDefault(); load(); }; return ( <div> <h3 style={{ marginBottom: 12, fontSize: 16, fontWeight: 600 }}>Журнал SMS ({total})</h3> <form onSubmit={handleSearch} style={{ marginBottom: 12, display: 'flex', gap: 8 }}> <input value={search} onChange={(e) => setSearch(e.target.value)} placeholder='Поиск по номеру или тексту...' style={{ flex: 1, padding: '8px 12px', borderRadius: 8, border: '1px solid #ddd', fontSize: 14 }} /> <button type='submit' style={{ padding: '8px 16px', borderRadius: 8, border: 'none', background: '#2563eb', color: '#fff', fontSize: 14, cursor: 'pointer' }}> Найти </button> </form> {loading ? <div>Загрузка...</div> : ( <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}> {items.map((sms) => ( <div key={sms.id} style={{ padding: 12, borderRadius: 8, background: 'var(--bg-input)', border: '1px solid var(--border-color)' }}> <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 4 }}> <span style={{ fontWeight: 600, fontSize: 14 }}>{sms.from} → {sms.to}</span> <span style={{ fontSize: 12, color: 'var(--text-secondary)' }}> {new Date(sms.receivedAt).toLocaleString('ru-RU')} </span> </div> <div style={{ fontSize: 14, color: 'var(--text-primary)', whiteSpace: 'pre-wrap' }}>{sms.text}</div> {sms.contact && ( <div style={{ marginTop: 4, fontSize: 12 }}> Контакт: <a href={`/contacts/${sms.contact.id}`}>{sms.contact.name}</a> </div> )} </div> ))} {items.length === 0 && <div style={{ color: 'var(--text-secondary)', fontSize: 14 }}>SMS не найдены</div>} </div> )} </div> ); }