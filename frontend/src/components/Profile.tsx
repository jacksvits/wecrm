import { useEffect, useState } from 'react' ; import { api } from '../api/client' ; import { User } from '../types' ; import { useAuth } from '../hooks/useAuth' ; import { AvatarUpload } from './AvatarUpload' ; import { usePush } from '../hooks/usePush' ; export function Profile() { const { logout } = useAuth() ; const { supported, subscribed, subscribe, unsubscribe } = usePush() ; const [user, setUser] = useState<User | null>(null) ; const [activeTab, setActiveTab] = useState<'profile' | 'notifications'>('profile') ; const [name, setName] = useState('') ; const [email, setEmail] = useState('') ; const [username, setUsername] = useState('') ; const [emails, setEmails] = useState<string[]>([]) ; const [newEmail, setNewEmail] = useState('') ; const [hideCompletedTasks, setHideCompletedTasks] = useState(false) ; const [darkTheme, setDarkTheme] = useState(false) ; const [pushEnabled, setPushEnabled] = useState(true) ; const [notifyTask, setNotifyTask] = useState(true) ; const [notifyComment, setNotifyComment] = useState(true) ; const [notifyChat, setNotifyChat] = useState(true) ; const [notifyCall, setNotifyCall] = useState(true) ; const [notifyDeal, setNotifyDeal] = useState(true) ; const [notifyNews, setNotifyNews] = useState(true) ; const [soundEnabled, setSoundEnabled] = useState(true) ; const [currentPassword, setCurrentPassword] = useState('') ; const [newPassword, setNewPassword] = useState('') ; const [confirmPassword, setConfirmPassword] = useState('') ; const [message, setMessage] = useState('') ; const [error, setError] = useState('') ; useEffect(() => { api.profile.get().then(u => { setUser(u) ; setName(u.name) ; setEmail(u.email) ; setUsername(u.username || '') ; setEmails(u.emails || []) ; setHideCompletedTasks(u.hideCompletedTasks ?? false) ; setDarkTheme(u.darkTheme ?? false) ; setPushEnabled(u.pushEnabled ?? true) ; setNotifyTask(u.notifyTask ?? true) ; setNotifyComment(u.notifyComment ?? true) ; setNotifyChat(u.notifyChat ?? true) ; setNotifyCall(u.notifyCall ?? true) ; setNotifyDeal(u.notifyDeal ?? true) ; setNotifyNews(u.notifyNews ?? true) ; setSoundEnabled(u.soundEnabled ?? true) ; }) ; }, []) ; const handleUpdateProfile = async (e: React.FormEvent) => { e.preventDefault() ; setMessage('') ; setError('') ; try { const updated = await api.profile.update({ name, email, username: username || null, emails, hideCompletedTasks, darkTheme, }) ; setUser(updated) ; localStorage.setItem('darkTheme', updated.darkTheme ? 'true' : 'false') ; window.dispatchEvent(new Event('themechange')) ; setMessage('Профиль обновлён') ; } catch (err: any) { setError(err.message || 'Ошибка обновления') ; } } ; const handleUpdateNotifications = async (e: React.FormEvent) => { e.preventDefault() ; setMessage('') ; setError('') ; try { const updated = await api.profile.update({ pushEnabled, notifyTask, notifyComment, notifyChat, notifyCall, notifyDeal, notifyNews, soundEnabled, }) ; setUser(updated) ; setMessage('Настройки уведомлений сохранены') ; } catch (err: any) { setError(err.message || 'Ошибка сохранения настроек') ; } } ; const handleChangePassword = async (e: React.FormEvent) => { e.preventDefault() ; setMessage('') ; setError('') ; if (newPassword !== confirmPassword) { setError('Пароли не совпадают') ; return ; } try { await api.profile.changePassword(currentPassword, newPassword) ; setMessage('Пароль изменён') ; setCurrentPassword('') ; setNewPassword('') ; setConfirmPassword('') ; } catch (err: any) { setError(err.message || 'Ошибка смены пароля') ; } } ; const handleAvatarUpdate = (avatar: string | null) => { setUser(prev => prev ? { ...prev, avatar } : null) ; } ; const addEmail = () => { const trimmed = newEmail.trim() ; if (!trimmed) return ; if (emails.includes(trimmed)) { setError('Этот email уже добавлен') ; return ; } setEmails([...emails, trimmed]) ; setNewEmail('') ; setError('') ; } ; const removeEmail = (idx: number) => { setEmails(emails.filter((_, i) => i !== idx)) ; } ; if (!user) return <div>Загрузка...</div> ; const isDark = darkTheme ; const cardBg = isDark ? '#2a2a35' : '#f9f9f9' ; const borderColor = isDark ? '#3f3f55' : '#e5e5e5' ; const tabStyle = (active: boolean): React.CSSProperties => ({ flex: 1, padding: '10px 16px', borderRadius: 10, border: 'none', background: active ? '#0077ff' : isDark ? 'rgba(255,255,255,0.08)' : '#f0f0f0', color: active ? '#fff' : 'var(--text-secondary)', fontSize: 14, cursor: 'pointer', fontWeight: active ? 600 : 400, transition: 'all 0.2s ease', }) ; const checkboxRow = (label: string, checked: boolean, onChange: (v: boolean) => void, disabled?: boolean) => ( <div style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '8px 0', opacity: disabled ? 0.5 : 1 }}> <input type='checkbox' id={label} checked={checked} onChange={e => onChange(e.target.checked)} disabled={disabled} style={{ width: 18, height: 18, cursor: disabled ? 'not-allowed' : 'pointer', accentColor: '#0077ff' }} /> <label htmlFor={label} style={{ fontSize: 14, cursor: disabled ? 'not-allowed' : 'pointer', flex: 1 }}>{label}</label> </div> ) ; return ( <div style={{ maxWidth: 520, margin: '0 auto', padding: '0 16px' }}> <h2 style={{ fontSize: 22, marginBottom: 12, fontWeight: 700 }}>Профиль</h2> {message && <div style={{ padding: 14, borderRadius: 12, background: '#dcfce7', color: '#166534', marginBottom: 14, fontSize: 14, fontWeight: 500 }}>{message}</div>} {error && <div style={{ padding: 14, borderRadius: 12, background: '#fee2e2', color: '#991b1b', marginBottom: 14, fontSize: 14, fontWeight: 500 }}>{error}</div>} <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12, marginBottom: 20, padding: 24, borderRadius: 16, background: cardBg, border: `1px solid ${borderColor}` }}> <AvatarUpload name={user.name} avatar={user.avatar} onUpdate={handleAvatarUpdate} size={100} /> <div style={{ fontSize: 13, color: 'var(--text-muted)' }}>Нажмите на аватар, чтобы изменить</div> {user.lastActiveAt && ( <div style={{ fontSize: 12, color: 'var(--text-muted)', marginTop: 4 }}> Последняя активность: {new Date(user.lastActiveAt).toLocaleString('ru')} </div> )} </div> <div style={{ display: 'flex', gap: 8, marginBottom: 20 }}> <button onClick={() => setActiveTab('profile')} style={tabStyle(activeTab === 'profile')}>Профиль</button> <button onClick={() => setActiveTab('notifications')} style={tabStyle(activeTab === 'notifications')}>Уведомления</button> </div> {activeTab === 'profile' && ( <form onSubmit={handleUpdateProfile} style={{ display: 'flex', flexDirection: 'column', gap: 12, marginBottom: 24 }}> <div> <label style={{ display: 'block', fontSize: 13, color: 'var(--text-secondary)', marginBottom: 4 }}>Имя</label> <input value={name} onChange={e => setName(e.target.value)} style={{ width: '100%', padding: '10px 12px', borderRadius: 8, border: '1px solid #ddd', fontSize: 14, background: 'var(--bg-input, #fff)', color: 'var(--text-primary)' }} /> </div> <div> <label style={{ display: 'block', fontSize: 13, color: 'var(--text-secondary)', marginBottom: 4 }}>Email</label> <input value={email} onChange={e => setEmail(e.target.value)} style={{ width: '100%', padding: '10px 12px', borderRadius: 8, border: '1px solid #ddd', fontSize: 14, background: 'var(--bg-input, #fff)', color: 'var(--text-primary)' }} /> </div> <div> <label style={{ display: 'block', fontSize: 13, color: 'var(--text-secondary)', marginBottom: 4 }}>Логин</label> <input value={username} onChange={e => setUsername(e.target.value)} placeholder='Не задан' style={{ width: '100%', padding: '10px 12px', borderRadius: 8, border: '1px solid #ddd', fontSize: 14, background: 'var(--bg-input, #fff)', color: 'var(--text-primary)' }} /> </div> <div> <label style={{ display: 'block', fontSize: 13, color: 'var(--text-secondary)', marginBottom: 4 }}>Дополнительные email</label> <div style={{ display: 'flex', gap: 8, marginBottom: 8 }}> <input type='email' value={newEmail} onChange={e => setNewEmail(e.target.value)} placeholder='Добавить email' style={{ flex: 1, padding: '10px 12px', borderRadius: 8, border: '1px solid #ddd', fontSize: 14, background: 'var(--bg-input, #fff)', color: 'var(--text-primary)' }} onKeyDown={e => { if (e.key === 'Enter') { e.preventDefault() ; addEmail() ; } }} /> <button type='button' onClick={addEmail} style={{ padding: '10px 16px', borderRadius: 8, border: 'none', background: '#0077ff', color: '#fff', fontSize: 14, cursor: 'pointer' }}>+</button> </div> {emails.length > 0 && ( <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}> {emails.map((em, idx) => ( <div key={idx} style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', padding: '6px 10px', borderRadius: 6, background: isDark ? '#353545' : '#f0f0f0', fontSize: 13 }}> <span>{em}</span> <button type='button' onClick={() => removeEmail(idx)} style={{ background: 'none', border: 'none', color: '#dc2626', cursor: 'pointer', fontSize: 16, lineHeight: 1 }}>&times;</button> </div> ))} </div> )} </div> <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}> <input type='checkbox' id='hideCompleted' checked={hideCompletedTasks} onChange={e => setHideCompletedTasks(e.target.checked)} /> <label htmlFor='hideCompleted' style={{ fontSize: 14 }}>Скрывать выполненные задачи</label> </div> <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}> <input type='checkbox' id='darkTheme' checked={darkTheme} onChange={e => setDarkTheme(e.target.checked)} /> <label htmlFor='darkTheme' style={{ fontSize: 14 }}>Тёмная тема</label> </div> <button type='submit' style={{ padding: '10px 16px', borderRadius: 8, background: '#0077ff', color: '#fff', border: 'none', fontSize: 14, cursor: 'pointer', fontWeight: 500 }}>Сохранить профиль</button> </form> )} {activeTab === 'notifications' && ( <form onSubmit={handleUpdateNotifications} style={{ display: 'flex', flexDirection: 'column', gap: 4, marginBottom: 24 }}> <div style={{ padding: 16, borderRadius: 12, background: cardBg, border: `1px solid ${borderColor}`, marginBottom: 12 }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 12, color: 'var(--text-primary)' }}>Push-уведомления в браузере</h3> {supported ? ( <div style={{ display: 'flex', alignItems: 'center', gap: 12, marginBottom: 12 }}> {subscribed ? ( <button type='button' onClick={unsubscribe} style={{ padding: '8px 16px', borderRadius: 8, border: '1px solid var(--border-color)', background: 'transparent', color: 'var(--text-muted)', fontSize: 13, cursor: 'pointer', display: 'flex', alignItems: 'center', gap: 6 }}> <svg width='16' height='16' viewBox='0 0 24 24' fill='none' stroke='currentColor' strokeWidth='2'> <path d='M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9' /> <path d='M13.73 21a2 2 0 0 1-3.46 0' /> <line x1='1' y1='1' x2='23' y2='23' /> </svg> Push ON — нажмите чтобы отключить </button> ) : ( <button type='button' onClick={subscribe} style={{ padding: '8px 16px', borderRadius: 8, border: 'none', background: '#0077ff', color: '#fff', fontSize: 13, cursor: 'pointer', display: 'flex', alignItems: 'center', gap: 6 }}> <svg width='16' height='16' viewBox='0 0 24 24' fill='none' stroke='currentColor' strokeWidth='2'> <path d='M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9' /> <path d='M13.73 21a2 2 0 0 1-3.46 0' /> </svg> Push OFF — нажмите чтобы включить </button> )} </div> ) : ( <div style={{ fontSize: 13, color: 'var(--text-muted)', marginBottom: 12 }}>Push-уведомления не поддерживаются в этом браузере</div> )} {checkboxRow('Включить push-уведомления', pushEnabled, setPushEnabled, !subscribed)} <div style={{ fontSize: 12, color: 'var(--text-muted)', marginTop: 4, marginBottom: 8 }}> {pushEnabled ? 'Push-уведомления активны' : 'Push-уведомления отключены'} </div> </div> <div style={{ padding: 16, borderRadius: 12, background: cardBg, border: `1px solid ${borderColor}`, marginBottom: 12 }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 12, color: 'var(--text-primary)' }}>Типы уведомлений</h3> {checkboxRow('Задачи (назначение, изменения)', notifyTask, setNotifyTask, !pushEnabled)} {checkboxRow('Комментарии к задачам', notifyComment, setNotifyComment, !pushEnabled)} {checkboxRow('Сообщения в общем чате', notifyChat, setNotifyChat, !pushEnabled)} {checkboxRow('Звонки', notifyCall, setNotifyCall, !pushEnabled)} {checkboxRow('Сделки', notifyDeal, setNotifyDeal, !pushEnabled)} {checkboxRow('Новости', notifyNews, setNotifyNews, !pushEnabled)} </div> <div style={{ padding: 16, borderRadius: 12, background: cardBg, border: `1px solid ${borderColor}`, marginBottom: 12 }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 12, color: 'var(--text-primary)' }}>Звук</h3> {checkboxRow('Звуковое уведомление в чате', soundEnabled, setSoundEnabled)} <div style={{ fontSize: 12, color: 'var(--text-muted)', marginTop: 4 }}> Воспроизводить звук при новых сообщениях в общем чате </div> </div> <button type='submit' style={{ padding: '10px 16px', borderRadius: 8, background: '#0077ff', color: '#fff', border: 'none', fontSize: 14, cursor: 'pointer', fontWeight: 500 }}>Сохранить настройки уведомлений</button> </form> )} <h3 style={{ fontSize: 16, marginBottom: 12, fontWeight: 600 }}>Смена пароля</h3> <form onSubmit={handleChangePassword} style={{ display: 'flex', flexDirection: 'column', gap: 12, marginBottom: 24 }}> <input type='password' placeholder='Текущий пароль' value={currentPassword} onChange={e => setCurrentPassword(e.target.value)} style={{ width: '100%', padding: '10px 12px', borderRadius: 8, border: '1px solid #ddd', fontSize: 14, background: 'var(--bg-input, #fff)', color: 'var(--text-primary)' }} /> <input type='password' placeholder='Новый пароль' value={newPassword} onChange={e => setNewPassword(e.target.value)} style={{ width: '100%', padding: '10px 12px', borderRadius: 8, border: '1px solid #ddd', fontSize: 14, background: 'var(--bg-input, #fff)', color: 'var(--text-primary)' }} /> <input type='password' placeholder='Подтвердите пароль' value={confirmPassword} onChange={e => setConfirmPassword(e.target.value)} style={{ width: '100%', padding: '10px 12px', borderRadius: 8, border: '1px solid #ddd', fontSize: 14, background: 'var(--bg-input, #fff)', color: 'var(--text-primary)' }} /> <button type='submit' style={{ padding: '10px 16px', borderRadius: 8, background: '#0077ff', color: '#fff', border: 'none', fontSize: 14, cursor: 'pointer', fontWeight: 500 }}>Изменить пароль</button> </form> <div style={{ borderTop: '1px solid #eee', paddingTop: 16, marginTop: 8 }}> <button onClick={logout} style={{ width: '100%', padding: '12px 16px', borderRadius: 8, background: '#dc2626', color: '#fff', border: 'none', fontSize: 14, cursor: 'pointer', fontWeight: 500, }} > Выйти из аккаунта </button> </div> </div> ) ; } 